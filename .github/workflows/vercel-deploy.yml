name: Deploy to Vercel

on:
  push:
    branches: [web]
  workflow_dispatch:

jobs:
  webhook:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy hook
        run: curl --fail --silent --show-error --request POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"